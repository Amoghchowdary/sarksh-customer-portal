# V10 Customer Authentication

Customer login is password + mandatory email OTP. Password verification does not create a session. Session creation occurs only after the OTP challenge succeeds. Pending-KYC customers remain allowed into the portal so they can complete KYC after login.
